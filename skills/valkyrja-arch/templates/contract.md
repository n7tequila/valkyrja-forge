---
contract: <kebab-case 契约名>      # 消费方引用格式：<契约名>@<version>
version: 1                         # 递增整数，每次修订 +1
date: <YYYY-MM-DD>
adec: ADEC-<DOMAIN>-<NNN>          # 定义/最近一次修订本契约的决策
---

# 契约：<名称>

<!-- 契约描述接口形状与语义（字段、格式、兼容规则），不写实现。
     实现属各 change 的 design/代码。
     修订流程：contract 特权握手 → version+1 → Changelog 记破坏性 → 回显消费方影响。 -->

## 用途与消费方

<这份契约约定了什么；哪些 capability / change 依赖它（首次定义时的已知清单，
后续消费方由 status 现算，不在此维护）。>

## 形状

<字段表 / 结构定义 / 示例。机器要解析的部分给出精确格式（正则或 schema 片段）。>

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| <field> | <type> | 是/否 | <语义、取值边界> |

## 兼容规则

<新增字段是否破坏；消费方应如何对待未知字段；版本协商方式（若有）。>

## Changelog

| 版本 | 日期 | 变更 | 破坏性 |
|---|---|---|---|
| 1 | <YYYY-MM-DD> | 初版 | — |
