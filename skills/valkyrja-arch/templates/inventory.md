# 公共对象清单（inventory）

> **目的：防止 AI 重复生成已有实现。** 编码前先查本清单；需要的类型/工具/组件
> 已存在时直接使用，不得在别处重新定义。
> 本清单是**事实登记**（什么已存在、在哪、怎么用），不是决策——
> 提取判断标准见文末，提取动作本身经 checkpoint 确认后登记。

## 已有公共对象

<!-- 按包/目录分组。每条：类型、名称、路径、一句话用途、（必要时）最小用法示例。
     路径必须真实——check 会验证存在性。 -->

### <分组名，如 共享内核 / API 层 / 工具>

| 类型 | 名称 | 路径 | 用途 |
|---|---|---|---|
| <class/record/组件/工具函数> | <名称> | <仓库相对路径> | <一句话> |

## 已建立的模式（勿重复发明）

<!-- 不是单个对象、而是"所有同类都照此写"的模式，附标准模板代码。
     例：ID 值对象模式、聚合根继承模式、分页流转模式。 -->

### <模式名>

<适用场景一句话>

```
<标准模板代码>
```

## 提取判断标准

| 条件 | 结论 |
|---|---|
| 被 2 个及以上模块直接引用 | 提取为公共对象并登记 |
| 纯技术语义（分页、错误、工具） | 提取并登记 |
| 有业务语义但仅单模块使用 | 保留模块本地，不登记 |
| 结构相似但代表不同领域概念 | 各自保留（如各模块 Status 枚举） |

## 刻意不提取的（防误判重复）

<!-- 看起来通用、实际属各自上下文的类型，登记在此防止后来者"好心合并"。 -->

| 类型 | 不提取原因 |
|---|---|
| <名称> | <原因> |
