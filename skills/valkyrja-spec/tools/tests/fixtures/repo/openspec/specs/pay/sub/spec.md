# pay/sub Specification

## Purpose

夹具嵌套主 spec：capability 身份是 specs/ 下完整相对路径 pay/sub（OpenSpec 归档
按完整路径合并，实测见 references/openspec-compatibility.md）。若 trace 按
basename 压平为 sub，将查不到本文件而误报 V4.3。

## Requirements

### Requirement: Nested Target
Sources: REQ-T-002

#### Scenario: n
- WHEN x
- THEN y
